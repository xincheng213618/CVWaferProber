<Window x:Class="CVWaferProber.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ChipMapping.Views;assembly=ChipMapping"
        xmlns:cvimg="clr-namespace:CVWPFCameraImage;assembly=CVWPFCameraImage"
        xmlns:cv="clr-namespace:CVWaferProber.Converters"
        xmlns:vm="clr-namespace:CVWaferProber.ViewModels"
        Title="晶圆台系统" Height="1000" Width="1200">

    <Window.Resources>
        <cv:StatusToColorConverter x:Key="StatusColorConverter"/>
    </Window.Resources>
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="550"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 左上：Mapping控件 -->
        <Border Grid.Row="0" Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="0">
            <local:ChipMappingControl x:Name="MyChipMappingControl" DataContext="{Binding CustomVM}"/>
        </Border>

        <!-- 左下：结果列表 -->
        <Border Grid.Row="1" Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="180"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- 操作区域 -->
                <Border Grid.Row="0" 
                    Background="#FFF0F0F0"
                    BorderBrush="#FFCCCCCC"
                    BorderThickness="1"
                    Padding="2"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top">

                    <!-- 在局部资源中定义转换器 -->
                    <Border.Resources>
                        <cv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
                    </Border.Resources>
                    
                    <StackPanel Orientation="Vertical">
                        <!-- 设置统一的边距来模拟间距 -->
                        <StackPanel.Resources>
                            <Style TargetType="Border">
                                <Setter Property="Margin" Value="0,0,0,10"/>
                            </Style>
                        </StackPanel.Resources>

                        <!-- 文件操作区域 -->
                        <Border CornerRadius="6" Background="#FFFFFFFF" Padding="5">
                            <DockPanel LastChildFill="True">
                                <Button x:Name="BtnOpenFile" Content="{DynamicResource BtnOpenFile}" 
                                    Width="80" 
                                    IsEnabled="{Binding IsNotProcessing}"
                                    Command="{Binding OpenMappingFileCommand}" 
                                    DockPanel.Dock="Left" 
                                    Margin="0,0,8,0"
                                    Background="#FF0078D7"
                                    Foreground="White"
                                    BorderThickness="0"/>

                                <Button x:Name="BtnClear" Content="{DynamicResource BtnClear}" 
                                    Width="80" 
                                    IsEnabled="{Binding IsNotProcessing}"
                                    Command="{Binding ClearMappingCommand}" 
                                    DockPanel.Dock="Right" 
                                    Margin="8,0,0,0"
                                    Background="#FF6C757D"
                                    Foreground="White"
                                    BorderThickness="0"/>

                                <Button x:Name="BtnRefresh" Content="{DynamicResource BtnRefresh}" 
                                    Width="80" 
                                    IsEnabled="{Binding IsNotProcessing}"
                                    Command="{Binding LoadMappingFileCommand}" 
                                    DockPanel.Dock="Right" 
                                    Margin="0,0,8,0"
                                    Background="#FF0D6EFD"
                                    Foreground="White"
                                    BorderThickness="0"/>

                                <Border CornerRadius="4"
                                    Background="#FFFFFFFF"
                                    BorderThickness="1"
                                    BorderBrush="#FFDDDDDD"
                                    VerticalAlignment="Center"
                                    Margin="8,0">
                                    <DockPanel>
                                        <TextBlock Text="{Binding ProberId}" Width="100" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="MappingCsvFilePath" 
                                       Text="{Binding MappingCsvFilePath}" 
                                       Margin="8,6"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"
                                       Foreground="#FF666666"/>
                                    </DockPanel>
                                </Border>
                            </DockPanel>
                        </Border>

                        <!-- 测量操作区域 -->
                        <Border CornerRadius="6"
                            Background="#FFFFFFFF"
                            Padding="5"
                            Height="90"
                            Margin="0,0,0,0">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Height="30" Margin="5,5">
                                    <Button Content="{DynamicResource BtnReRegist}" Command="{Binding RCRegCommand}"  IsEnabled="{Binding IsNotProcessing}" Width="120" Margin="0,0,10,0"/>
                                    <ComboBox Width="250" Height="30" ItemsSource="{Binding FlowItems}" SelectedItem="{Binding SelectedFlow, Mode=TwoWay}"  IsEnabled="{Binding IsNotProcessing}" VerticalAlignment="Center">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" Width="150"/>
                                                    <TextBlock Text=" (ID: " Foreground="Blue"/>
                                                    <TextBlock Text="{Binding Id}" Foreground="Blue"/>
                                                    <TextBlock Text=")" Foreground="Blue"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                    <Button Content="{DynamicResource BtnFlowLoad}" Command="{Binding FlowLoadCommand}" Width="120" IsEnabled="{Binding IsNotProcessing}" Margin="10,0,0,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Height="30" Margin="5,5">
                                    <!-- 使用Margin来设置间距 -->
                                    <Button Content="{DynamicResource BtnStartAutoTest}" 
                                        Width="110" 
                                        Command="{Binding StartAutoTestCommand}" 
                                        BorderThickness="0"
                                        Margin="0,0,10,0"/>

                                    <Button Content="{DynamicResource BtnStopAutoTest}" 
                                            Width="110" 
                                            Command="{Binding StopAutoTestCommand}" 
                                            BorderThickness="0"/>

                                    <Button Content="{DynamicResource BtnRefreshStatus}" 
                                            Width="110" 
                                            Command="{Binding RefreshStatusCommand}" 
                                            BorderThickness="0"
                                            Margin="10,0,0,0"/>

                                    <CheckBox Content="{DynamicResource ChkColorStatus}" IsChecked="{Binding IsColorEnabled}" Margin="10,5,10,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Height="30" Margin="5,5">
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- 状态指示 -->
                        <Border CornerRadius="6" Background="#FFFFFFFF" Padding="10" Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="12" Height="12" Fill="#FF198754">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform x:Name="SpinnerTransform" Angle="0" CenterX="6" CenterY="6"/>
                                    </Ellipse.RenderTransform>
                                    <Ellipse.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="SpinnerTransform" 
                                                     Storyboard.TargetProperty="Angle"
                                                     From="0" To="360" Duration="0:0:1" 
                                                     RepeatBehavior="Forever"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Ellipse.Triggers>
                                </Ellipse>
                                <TextBlock Text="自动测量运行中..." Foreground="#FF666666" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <DataGrid x:Name="TestResultDataGrid" Grid.Row="1" ItemsSource="{Binding TestResults}" 
                          AutoGenerateColumns="False" IsReadOnly="True"
                          SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          SelectionMode="Single" SelectionUnit="FullRow"
                          Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="序号" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="MapYRow" Binding="{Binding MapY}" Width="60"/>
                        <DataGridTextColumn Header="MapXCol" Binding="{Binding MapX}" Width="60"/>
                        <DataGridTextColumn Header="屏幕X坐标" Binding="{Binding ScreenX}" Width="60"/>
                        <DataGridTextColumn Header="屏幕Y坐标" Binding="{Binding ScreenY}" Width="60"/>
                        <DataGridTextColumn Header="序列号" Binding="{Binding SerialNumber}" Width="200"/>
                        <DataGridTextColumn Header="开始时间" Binding="{Binding StartTestTime}" Width="180"/>
                        <DataGridTextColumn Header="结束时间" Binding="{Binding EndTestTime}" Width="180"/>
                        <DataGridTextColumn Header="测试状态" Binding="{Binding DisplayStatus}" Width="*"/>
                        <DataGridTextColumn Header="均匀性" Binding="{Binding DataValue}" Width="100"/>
                        <DataGridTextColumn Header="总用时" Binding="{Binding TotalTime}" Width="180"/>
                    </DataGrid.Columns>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="{Binding Status, Converter={StaticResource StatusColorConverter}}"/>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </Grid>
        </Border>

        <!-- 右侧区域（保持不变） -->
        <Grid Grid.Column="1" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 右上相机图像 -->
            <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="相机图像" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>

                    <cvimg:CVCameraImageViewer x:Name="MyImageControl" DataContext="{Binding CustomImageVM}"/>
                </Grid>
            </Border>

            <!-- 右下图表 -->
            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="测试结果图表" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>

                    <ContentControl Grid.Row="1" Content="{Binding ChartViewModel}" Margin="5"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>